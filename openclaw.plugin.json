{
  "id": "nocturne-memory",
  "kind": "non-capability",
  "name": "Nocturne Memory",
  "description": "Multi-agent long-term memory powered by Nocturne Memory with namespace isolation. Each agent gets its own isolated memory space within a single NM instance.",
  "configSchema": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "Nocturne Memory SSE server base URL (e.g. http://localhost:8000)"
      },
      "agents": {
        "type": "array",
        "description": "Per-agent namespace mappings",
        "items": {
          "type": "object",
          "properties": {
            "agentId": { "type": "string", "description": "OpenClaw agent ID" },
            "namespace": { "type": "string", "description": "NM namespace for this agent" }
          },
          "required": ["agentId", "namespace"]
        }
      },
      "defaultNamespace": {
        "type": "string",
        "description": "Fallback namespace for agents not in the list (default: empty string)",
        "default": ""
      }
    },
    "required": ["url"]
  }
}
